This little example shows how to stop a XHR request.

The code uses makes an XHR request to `localhost:3000`, which is then intercepted and stubbed with `cy.server` and `cy.route`.

![Animation](animation.gif)
